神经网络本质上是一个可以自动调整参数的函数。但是参数不是靠你来写，而是通过训练学出来，而训练的意义就是让输出越来越接近正确答案
偏置 `b` 可以理解成一个“整体调整项”。如果没有 `b`，函数必须经过原点。加上 `b` 后，模型表达能力更强。
神经网络有输入层，隐藏层，输出层
输入层负责接受原始数据
隐藏层负责提取特征
输出层负责给出最终结果

`

损失函数定义了优化的目标（“地形”），而梯度下降是实现该目标的手段（“下山的方法”）