## 数组

### [704. 二分查找](https://leetcode.cn/problems/binary-search/)
lo
二分有两种写法，是 `while(left < right)` 还是 `while(left <= right)`，是`right = middle`呢，还是要`right = middle - 1`呢？
要根据右端点是否开来决定里面的具体写法


###   [27. 移除元素](https://leetcode.cn/problems/remove-element/)
暴力做法就是两重循环，一重确定值为target时，再来一重循环，使每个后续元素向前

使用快慢指针将n^2复杂度降到n
#todo [什么时候用双指针](https://chatgpt.com/s/t_69f3aa7c432081918c106ec30f88e0cd)
以后只要题目要求“原地修改数组”**、**“把满足/不满足某种条件的元素移到前面/后面”**，或者**“去重”，**试试双指针！**
### [977. 有序数组的平方](https://leetcode.cn/problems/squares-of-a-sorted-array/)
观察出每次的最大值一定来自两端，两个指针一个从左一个从右来循环

### [209. 长度最小的子数组](https://leetcode.cn/problems/minimum-size-subarray-sum/)
滑动窗口
面对这种暴力应该是n方级别的数组题，可以使用双指针


### [59. 螺旋矩阵 II](https://leetcode.cn/problems/spiral-matrix-ii/)
使用方向数组 `dx` 和 `dy` 来控制移动方向,值得注意的是每次撞墙后，确实只要转一次弯（ch = (ch + 1) % 4）新方向就一定是通的。

### 58. 区间和（第九期模拟笔试）
前缀和
涉及问区间元素数量和可以使用

### [209. 长度最小的子数组](https://leetcode.cn/problems/minimum-size-subarray-sum/)
如果暴力就是n * 2的复杂度，可以使用一种双指针去进行优化
首先就是通过记录每次符合条件的长度，然后取最小即可，
窗口的起始位置如何移动：如果当前窗口的值大于等于s了，窗口就要向前移动了（也就是该缩小了）。

窗口的结束位置如何移动：窗口的结束位置就是遍历数组的指针，也就是for循环里的索引。
## 链表
### 链表的构造函数
```c++
struct ListNode {
    int val;  // 节点上存储的元素
    ListNode *next;  // 指向下一个节点的指针
    ListNode(int x): val(x),next(null) {}
};
```

###   [203. 移除链表元素](https://leetcode.cn/problems/remove-linked-list-elements/)
设置一个虚拟头节点：好处就是处理头节点和中间节点时代码就可以通用了


### [707. 设计链表](https://leetcode.cn/problems/design-linked-list/)
#todo 复习链表必做，考察很全面
上次没考虑到的优化是对于增加节点来说可以只写一个addatIndex的函数，在add头和尾节点函数去调用这个函数即可


### [206. 反转链表](https://leetcode.cn/problems/reverse-linked-list/)
用三个指针，cur,pre,tem，tem指向当前节点的原本下个节点，pre指向当前节点的上个节点，在依次调转

###   [24. 两两交换链表中的节点](https://leetcode.cn/problems/swap-nodes-in-pairs/)

直接模拟就好，不好想象的时候画图即可，要注意终止条件，就是当前节点的后两个节点不为空即可

### [19. 删除链表的倒数第 N 个结点](https://leetcode.cn/problems/remove-nth-node-from-end-of-list/)
第一想法是遍历一共有多少个节点，然后从正者数看该是删第几个
但是需要多遍历一次，这种找倒数第几个的情况可以使用双指针，让快指针先走n+1，当fast到空指针时，假设共有m个节点，因为有虚拟头指针的存在，因此走了m+1次，此时慢指针走了m-n次，即是正数m-n个节点，是倒数m+1-(m-n)个节点，即第n+1个节点
此时的cur->next是要删除的

## 哈希表
遇到了要快速判断一个元素是否出现集合里的时候，就要考虑哈希法。
### 242.有效的字母异位词
因为只有小写字母，也就是用数组去模拟哈希表效率很高，只需要开26大小，然后数组A的字母有多少加进去，数组B的字母相应减回去，数组不为空说明不是字母异位词
***
数组也是一种哈希表，在面对明确知道tablesize时可以直接使用数组来模拟
如果哈希值比较少、特别分散、跨度非常大，使用数组就造成空间的极大浪费。

### 349. 两个数组的交集
因为两个数组都可能有重复元素，可以先将数组A转为set去重，接下来只需要遍历数组B的元素，如果里面元素与数组A有相同的，就可以放到答案set中即可
**`end()`**:
- 返回一个指向容器**最后一个元素之后的位置**的迭代器。
- 它**不指向任何有效元素**，只是一个“哨兵”或“结束标记”。

### [202. 快乐数](https://leetcode.cn/problems/happy-number/)
抽屉原理（Pigeonhole Principle）
> 如果你有无限次操作，但只有有限个可能的状态，那么至少有一个状态会被重复进入。
在这道题中，由于是int类型，假设位数为10（int整型最大为10），假设每位数都是9，那平方和最大为810，因此后续的平方和都会落在[0,810]之间，而一旦某个sum重复出现，就会从这个点开始一直循环。因此可以用set去存里面的每次的平方和数，若相同则能说明进入重复了


###   [1. 两数之和](https://leetcode.cn/problems/two-sum/)
按暴力的想法就是2重遍历，但是面对这种我们需要查询一个元素是否出现过，或者一个元素是否在集合里的时候，就要第一时间想到哈希法。
为了防止重复，可以把已经遍历过的元素存入哈希，因为这里要求的是下标，因此可以将key存数字是什么，value存下标，依次在遍历时判断前面是否有mp[target-i]如果有，则成立

### [454. 四数相加 II](https://leetcode.cn/problems/4sum-ii/)
因为结果要求的是有多少个元组能保证满足条件，没涉及到位置，因此可以将四数相加转换为两数相加的问题


### [383. 赎金信](https://leetcode.cn/problems/ransom-note/)
和242一模一样

### [15. 三数之和](https://leetcode.cn/problems/3sum/)
难点在于要去重，并且要做的是 不能有重复的三元组，但三元组内的元素是可以重复的
而去重可以分为a的去重和b,c的去重
对于a 的去重有两种写法
```c++
   if(nums[i]==nums[i-1])
        continue
	if(nums[i]==nums[i+1])
		continue;
```
当使用2写法时，例如{-1, -1 ,2} 这组数据，当遍历到第一个-1 的时候，判断 下一个也是-1，那这组数据就pass了。

对于b,c的去重
比较常规，即下一个元素和上一个相同就跳过
## 栈与队列

### STL
#### stack
基本操作
- `push()`: 在栈顶添加一个元素。
- `pop()`: 移除栈顶元素。
- `top()`: 返回栈顶元素的引用，但不移除它。
- `empty()`: 检查栈是否为空。
- `size()`: 返回栈中元素的数量。

#### queue
常用操作

队列提供了以下常用操作：

- `empty()`: 检查队列是否为空。
- `size()`: 返回队列中的元素数量。
- `front()`: 返回队首元素的引用。
- `back()`: 返回队尾元素的引用。
- `push()`: 在队尾添加一个元素。
- `pop()`: 移除队首元素。

### 232.用栈实现队列
首先队列和栈不一样的就是队列是先进先出
所以要用先进后出的结构实现，就可以使用两个栈，一个作为输入栈，一个作为输出栈。每次push都放到输入栈里，要查询队头就可以将输入栈里的元素全部取到输出栈里，就实现了先入的在栈的上面了，而且如果输出栈里有元素，就直接输出即可，当输入栈和输出栈都为空就可以判断队列为空了
***
### 225.用队列实现栈
一个队列在模拟栈弹出元素的时候只要将队列头部的元素（除了最后一个元素外） 重新添加到队列尾部，此时再去弹出元素就是栈的顺序了。

***

### 1047.有效的括号
括号匹配是使用栈解决的经典问题。
可以使用哈希表来存右括号，如果进的是左括号就直接进，碰到右括号就看能不能和栈顶匹配上，可以就出栈，不可以说明有错
当栈为空说明左括号消耗干净，括号有效
### 150.逆波兰表达式求值
==逆波兰表达式：是一种后缀表达式，所谓后缀就是指运算符写在后面。==
逆波兰表达式主要有以下两个优点：
- 去掉括号后表达式无歧义，上式即便写成 1 2 + 3 4 + * 也可以依据次序计算出正确结果。
- 适合用栈操作运算：遇到数字则入栈；遇到运算符则取出栈顶两个数字进行计算，并将结果压入栈中。
### 239.滑动窗口最大值
#单调队列
是一道模版题，如果使用暴力做法，时间复杂度为O(nk)，优化的思想为
假设一个数又在你后面，数值还比你大，在这个数进来之后，前面比他小的数就永远不可能成为最大值，因此我们可以使该这个队列为单调递减的一个队列，每次入队看是否队列中已经存在比该元素小的数，有就删除即可
出队则是看是否窗口已经越过队列中的最大值，过了就删除即可
队头始终是最大值

### 347.前k个高频元素
1. 要统计元素出现频率(unordered_map)
2. 对频率排序
3. 找出前K个高频元素
2.3 因素决定使用(优先队列)，只排部分最大，因此不需要使用排序算法对所有元素排序



## 二叉树
### 二叉树的递归遍历（前中后序遍历）

递归三要素
1. 确定函数的返回值和参数
2. 确定递归终止条件
3. 确定每层循环的逻辑

对于递归遍历
没有输出，参数为一个指针
终止条件就是当前节点为空节点
3 就是根据前中后遍历顺序不同调整一下代码顺序就可以


### 二叉树的迭代遍历
在我看来，所谓迭代和递归的区别就是递归是使用了系统提供的栈去完成，而迭代需要自己实现这个栈的过程
代码模版
==在node!=NULL里更改入栈顺序就可以得到不同的遍历顺序==
```c++
class Solution {
public:
    vector<int> inorderTraversal(TreeNode* root) {
        vector<int> result;
        stack<TreeNode*> st;
        if (root != NULL) st.push(root);
        while (!st.empty()) {
            TreeNode* node = st.top();
            if (node != NULL) {
                st.pop(); // 将该节点弹出，避免重复操作，下面再将右中左节点添加到栈中
                if (node->right) st.push(node->right);  // 添加右节点（空节点不入栈）

                st.push(node);                          // 添加中节点
                st.push(NULL); // 中节点访问过，但是还没有处理，加入空节点做为标记。

                if (node->left) st.push(node->left);    // 添加左节点（空节点不入栈）
            } else { // 只有遇到空节点的时候，才将下一个节点放进结果集
                st.pop();           // 将空节点弹出
                node = st.top();    // 重新取出栈中元素
                st.pop();
                result.push_back(node->val); // 加入到结果集
            }
        }
        return result;
    }
};
```

### 二叉树的层序遍历
队列先进先出，满足一层一层遍历的逻辑
层序遍历其实就是图论的广度优先搜索


## 字符串
### [344. 反转字符串](https://leetcode.cn/problems/reverse-string/)
与反转链表一样，都是使用双指针的方法

### [541. 反转字符串 II](https://leetcode.cn/problems/reverse-string-ii/)
一开始的逻辑：搞了个计数器来统计是否到达2k个字符
实际却没必要，遍历字符串时只需要让指针每次跳2k个即可
**当需要固定规律一段一段去处理字符串的时候，要想想在for循环的表达式上做做文章。**

### [替换数字](https://kamacoder.com/problempage.php?pid=1064)
字符串拼接直接使用+=即可
1. **`res[j++] = s[i];` 是错误的**：
    - `res` 是一个空字符串（初始长度为 0），你不能通过下标 `j` 直接赋值，这会导致**越界访问**，行为未定义。
    - 正确做法是使用 `push_back()` 或 `+=` 来追加字符。
2. **`res.insert(j, num);` 的用法不正确**：
    - `string::insert(pos, str)` 是将 `str` 插入到 `pos` 位置，**并不会自动增长字符串的“容量”用于后续下标访问**。
    - 而且你在 `insert` 后手动增加 `j += 6`，但后续仍然混合使用下标赋值，逻辑混乱。

这个题是可以不新开额外数组来完成的，两步遍历，第一次看有多少数字，然后对数组进行扩容，然后从后往前去改变，本质仍是双指针


## 贪心
题的时候，只要想清楚 局部最优 是什么，如果推导出全局最优，其实就够了。


### [455. 分发饼干](https://leetcode.cn/problems/assign-cookies/)
首先我们能发现，尽可能让饼干大小=胃口大小是最好的，即使不能满足这点，也是略大于是最好的，因此我们将胃口和饼干大小排序，让胃口小的以此被最合适的饼干满足，以局部最优（饼干略大于胃口）推全局最优（数量最多）


### [376. 摆动序列](https://leetcode.cn/problems/wiggle-subsequence/)

最长摆动序列的长度是就是上升+下降区间+1


### [53. 最大子数组和](https://leetcode.cn/problems/maximum-subarray/)
当前子数组之和为负数就清0，从下一个正数开始重新记


### [122. 买卖股票的最佳时机 II](https://leetcode.cn/problems/best-time-to-buy-and-sell-stock-ii/)
局部最优是每两天的利润最优


###   [55. 跳跃游戏](https://leetcode.cn/problems/jump-game/)
只需要判断能到达的最远位置是不是覆盖到了数组的最后一个下标位置即可



### [45. 跳跃游戏 II](https://leetcode.cn/problems/jump-game-ii/)
两种解决思路，用dp来做的话时间复杂度为n^2 优点是可以回溯每一步是怎么做的，但是题目只问了最少用几步，就可以使用贪心来做，每步的最优是看当前一步能到的最大范围是多少，第一次触及到终点就是最小步骤



### [1005. K 次取反后最大化的数组和](https://leetcode.cn/problems/maximize-sum-of-array-after-k-negations/)
 发现永远是应该先从小到大去翻，即最小负数，再0。在最小的正数



###  [134. 加油站](https://leetcode.cn/problems/gas-station/)
意见：题目中的顺序是指从右边的顺序，不是左右都可以

首先第一点，如果总体的汽油获取量小于消耗量一定不行，并且，作为起点，如果当前地点的获取量小于这段路的消耗量，这是绝对不能成为起点的，就从下一站考虑即可




### [135. 分发糖果](https://leetcode.cn/problems/candy/)
把问题拆分，先从左往右遍历满足右孩子>左
在从从右往左便利满足左>右
二者确定的糖果数取最大


### [860. 柠檬水找零](https://leetcode.cn/problems/lemonade-change/)
局部最优就是遇到20 先用10块的
错误原因是因为==不是全局变量记得要初始化==



### [406. 根据身高重建队列](https://leetcode.cn/problems/queue-reconstruction-by-height/)




## 字符串
### [344. 反转字符串](https://leetcode.cn/problems/reverse-string/)
一前一后两个指针

### [541. 反转字符串 II](https://leetcode.cn/problems/reverse-string-ii/)
多几个特殊判断
### 54. 替换数字（第八期模拟笔试）
简单
###  [151. 反转字符串中的单词](https://leetcode.cn/problems/reverse-words-in-a-string/)
```python
return " ".join(reversed(s.split()))
```
### 55. 右旋字符串（第八期模拟笔试）
记得取模就好

### [28. 找出字符串中第一个匹配项的下标](https://leetcode.cn/problems/find-the-index-of-the-first-occurrence-in-a-string/)
#kmp
[Chatgpt](https://chatgpt.com/s/t_6a2406d6e28c819197a053ddcb8fbb45)
### [459. 重复的子字符串](https://leetcode.cn/problems/repeated-substring-pattern/)
```python
return s in (s+s)[1:-1]
```
把s* 2，如果有子串t 能通过重复n次拼成s，那么拼成的 s s中，中间一定有连续的n次子串，
如果 $s$ 没办法由更短的子串重复构成，那么在双倍字符串 $s + s$ 中，完整的 $s$ 只可能出现在两个完美对齐的位置


## 动态规划
### [746. 使用最小花费爬楼梯](https://leetcode.cn/problems/min-cost-climbing-stairs/)
```
dp[0]=0
dp[1]=0
dp[i]=min(dp[i-2]+cost[i-2],dp[i-1]+cost[i-1])
```
### [62. 不同路径](https://leetcode.cn/problems/unique-paths/)
注意python语法中二维数组初始化要使用列表推导式
其次就是要把第一行和第一列都初始化
### [63. 不同路径 II](https://leetcode.cn/problems/unique-paths-ii/)
第一行或第一列只要出现障碍物，这一行/一列的后面都不可行
### [343. 整数拆分](https://leetcode.cn/problems/integer-break/)
1. 状态定义：`dp[i]` 表示将正整数 `i` 拆分为至少两个正整数的和时，能获得的最大乘积。
2. 转移方程结构：因为需要在所有可能的拆分方案中取最大值，所以等式右边必须包含自身进行比较，即 `dp[i] = max(dp[i], ...)`。
3. 两种拆分情况讨论：
    - 恰好拆成 2 个数：乘积为 `j * (i - j)`；
    - 拆成 3 个及以上数：相当于把 `i-j` 继续拆分取其最大乘积，即 `j * dp[i - j]`。
4. 遍历范围优化：由于乘法具有对称性（`j*(i-j)` 与 `(i-j)*j` 等价），为了避免重复计算，`j` 只需从 `1` 遍历到 `i / 2` 即可覆盖所有最优解。


### [96. 不同的二叉搜索树](https://leetcode.cn/problems/unique-binary-search-trees/)
dp[i]意味有i个节点能组成的二叉搜索树的个数，
当最大整数位n，以i为根节点时他的左子树的数量为dp[i-1]，右子树的组合数量为dp[n-i]
在递推函数为乘法时，dp[0]一般不能为0
