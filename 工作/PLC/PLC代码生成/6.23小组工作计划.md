谭启航、毛剑
1. 通读论文内容：整体阅读目标论文，了解其研究背景、系统架构与实现细节；
2. 分析关键步骤：重点关注文章中任务编排与执行的具体流程，明确每一步的逻辑和目的；
3. 探索替换空间：结合文章内容，思考在哪些环节可以尝试引入大模型，提升任务编排的智能化程度；

吕盈忱
- 家里有事，暂不开展工作




1.影响因子是不是看发表那年的
2.会议论文是不是不用写SCI分区

```mermaid
flowchart LR
  %% 死代码删除：基于活性分析的示例（扁平版）

  subgraph B[删除前：示例与活性信息]
    direction LR
    n1[1: X = 5\nlive-out = {}]
    n2[2: Y = 1\nlive-out = {Y}]
    n3[3: print Y\nlive-out = {}]
    n1 --> n2 --> n3
  end

  n1 -. 条件：X ∉ live-out(1) .-> drop[删除 1: X = 5（DCE）]

  subgraph A[删除后：程序语义不变]
    direction LR
    m2[1: Y = 1]
    m3[2: print Y]
    m2 --> m3
  end

  B -->|应用 DCE| A

  %% 轻量样式（可删）
  classDef dead fill:#fee,stroke:#c33,stroke-width:1px;
  class n1,drop dead;

```
